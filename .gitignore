/build/
/tags
/dist/

# Default --rerun-failed state file. Unanchored: it lands in whatever
# directory the binary was invoked from, not just the repo root. The glob
# also covers the .tmp staging sibling an interrupted write leaves behind.
.lfg-ctest-last*
